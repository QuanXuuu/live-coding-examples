// mit typeof koennen wir herausfinden, welchen typ ein wert hat.

// should be a number
typeof(50);

// should be a string
typeof('Hallo');

// should be a boolean
typeof(true);

// should be undefined
typeof(test); // 'undefined'