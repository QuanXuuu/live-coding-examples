// var
    var testValue1 = 5;

    // eine var kann redeklariet werden
    var testValue1 = 10;

    // eine var kann geupdated werden
    testValue1 = 20;

// let
let testValue2 = 5;

    // eine let kann upgedated werden
    testValue2 = 10;

    // eine let kann NICHT redeklariert werden
    //let testValue2 = 20;

// Const
    const testValue3 = 5;

    // eine const kan NICHT geupdated werden
    // eine const kann NICHT redeklariet werden

// ein beispiel fuer let:
    let zahl = 5;
    zahl = 8;
    console.log(zahl)

